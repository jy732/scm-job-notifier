# Copy to .env and fill in. .env is gitignored.
EMAIL_USERNAME=
EMAIL_APP_PASSWORD=
# Single recipient list — one alert email covers entry-level, internship, and unsure roles.
NOTIFICATION_EMAIL=
GEMINI_API_KEY=
